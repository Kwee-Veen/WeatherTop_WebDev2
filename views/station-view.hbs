{{> menu}}

<section class="section">
  <div class ="title">
    {{station.title}}
  </div>
  {{#each station}}
    {{> data-overview}}
  {{/each}}
  {{> list-readings}}
  {{> add-reading}}
</section>